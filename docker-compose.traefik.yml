version: '3.7'
services:
  django:
    environment:
    - HOST
    - ALLOWED_HOSTS=${HOST}
    - SENTRY_AUTH_TOKEN
    - SENTRY_DSN
    - SENTRY_ORG=sentry
    - SENTRY_PROJECT
    - SENTRY_URL
    - TEAM_EMAIL
    - BASICAUTH_ENABLE
    labels:
    - "traefik.enable=true"
    - "traefik.frontend.rule=Host: ${HOST}"
    - "traefik.port=8000"
    networks:
    - default
    - web

  postgres:
    networks:
    - default

networks:
  web:
    external: true
